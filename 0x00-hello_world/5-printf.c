#include <stdio.h>
/**
 * main - Prints a statement followed by a new line
 * The statement will begin with '
 * Return: a value of zero is returned
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
